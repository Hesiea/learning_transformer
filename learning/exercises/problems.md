# 题库：主动回忆练习

> **不要看 `solutions.md`**，先在这里写你的答案。
> 写不出来是有效信号——说明那一条其实还没掌握，比「读懂了」诚实得多。
>
> 出题原则：不给代码、只给场景。答案要求是**你的推理过程**，不是结论词。
> 答完让 Agent 对照 `solutions.md` 批改：它只指出缺漏，不直接给标准答案。

## 用法

1. 每次会话开头，让 Agent 从你 `progress.md` 里 🟠 及以上的条目里抽 1~2 题
2. 你在这里作答（连带日期）
3. Agent 批改后，错的抄进 `mistakes.md` 并排重考日期

---

## 基础题（对应 01~04 讲）

### P1 ｜ 状态：待作答

假设把一个训练充分的字符级模型，在**完全相同的输入**上前向两次，两次的 loss 会相同吗？
如果不同，可能来自哪些地方？如果模型处于 `eval()` 模式呢？

```
（在这里作答）
```

### P2 ｜ 状态：待作答

因果掩码用 `-inf` 填充未来位置之后过 softmax。为什么不能用 `0`？

请解释：如果用 0 会发生什么，并说明这个错误会在哪个环节暴露出来
（提示：想清楚 softmax 的输入是什么、`0` 代表什么含义）。

```
（在这里作答）
```

### P3 ｜ 状态：待作答

取 `n_embd=64, n_head=4`，即 `head_size=16`。03 讲说缩放因子是 `1/√d_k`。
问：这里的 `d_k` 是 64 还是 16？为什么不能是另一个？改错了会怎样？

```
（在这里作答）
```

---

## 进阶题（对应 05~09 讲）

### P4 ｜ 状态：待作答

训练到一半，你观察到的现象是：**训练 loss 从 4.17 降到 1.20，验证 loss 从 3.80 升到 4.00**。

问：这最可能是什么问题？举出两种能缓解它的手段，并说明各自的作用机制。
另外——有没有可能这其实不是过拟合？如果有，是什么情况？

```
（在这里作答）
```

### P5 ｜ 状态：待作答

KV Cache 在 `block_size=64` 的小模型上几乎没有收益（实测约 1.05x）。
假设你把 `block_size` 放大到 1024、模型层数也增加，加速比会怎么变？

要求：给出你的推理链（不要只说「会更快」），并说出**盈亏平衡点**大致由哪些量决定。

```
（在这里作答）
```

### P6 ｜ 状态：待作答（这道是陷阱题，请格外小心）

有人做了个实验：固定 `n_embd=128`，比较 `n_head=1/2/4/8` 的验证 loss，
发现「头数越少越好」，于是得出结论「多头注意力没用」。

问：这个实验的设计缺陷是什么？请**具体指出混淆变量**，
并给出一个能真正分离出「头数」影响的设计方案。

```
（在这里作答）
```

### P7 ｜ 状态：待作答

中文语料 `zh_poetry` 的词表是 8300，英文 TinyShakespeare 是 65。
所以两者的 loss 数值**不能直接比较**。

问：那要比较「模型在两种语言上学得好不好」，应该用什么指标？
请写出它与你熟悉的交叉熵之间的换算关系（能推导出来更好）。

```
（在这里作答）
```
