# 模板：实验记录

> 复制这份文件到 `experiments/NNNN_简短标题.md`，编号从 0001 开始递增。
> 文件名用中文没问题（本工程一直是中文），但要短、能一眼看懂在做什么。
>
> **最重要的一条**：第 2 节「预测」必须在**跑之前**写好。
> 如果你的预测和实际一致，说明心智模型没问题；不一致，那个差值就是你今天真正学到的东西。

---

## 1. 问题

一句话说清这个实验要回答什么。**一次只问一个问题**。
如果发现要问两个，就拆成两个实验。

## 2. 预测（跑之前写！）

| 我预测 | 理由（为什么这么想） |
|---|---|
|  |  |

> 如果写不出理由，说明这其实不是预测，是猜测 —— 那就先别跑，回去看原理。

## 3. 设计

- **自变量**（改什么）：
- **控制变量**（哪些必须固定）：种子、步数、batch size、block size、数据集、模型规模
- **因变量**（看什么指标）：验证 loss / 训练 loss / 梯度范数 / 生成样例 / 耗时
- **预计规模与耗时**：（CPU 上多少分钟）

## 4. 执行

```powershell
# 把实际跑的命令原样贴在这里，包括所有参数
```

关键原始输出（不要只贴结论，要贴数字）：

```
```

## 5. 对账

| 我预测 | 实际 | 差在哪 / 为什么 |
|---|---|---|
|  |  |  |

**如果预测错了**：写清楚是哪个环节的认知有问题（是公式记错了？是忽略了某个混淆变量？
还是对量级没有直觉？）—— 这一栏要诚实地写，它比结论更有价值。

## 6. 结论与新问题

- **一句话结论**：
- **这个结论的适用范围**：（在什么条件下成立？换个规模还成立吗？）
- **它引出的新问题**：（写进 `../questions.md`）
- **能升档的进度项**：（例如 A7 → 🟢，证据 = 本文件）
