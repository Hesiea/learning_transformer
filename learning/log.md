# 学习日志

> 写日志的规矩（很重要，决定了这份文件有没有价值）：
> **结论必须由学员本人写下**。Agent 代写的结论不算理解。
> Agent 的补充要单独标注，不要混进你的话里。

## 模板（每次会话结束复制一份）

```
## YYYY-MM-DD ｜ 第 N 次 ｜ 主题

**本次做了什么**
- （改了哪个文件 / 跑了哪条命令）

**我自己的结论（用自己的话，不看代码）**
- 

**我的预测 vs 实际**
| 我预测 | 实际 | 差在哪 |
|---|---|---|
|  |  |  |

**还没想通的**
- 

**下一步**
- 
```

---

## 2026-09-13 ｜ 第 0 次 ｜ 环境搭建与工程验收（Agent 代建，供参考格式）

**本次做了什么**
- 搭好工程内自带的 Python 环境（官方 CPython 3.12.10 + torch 2.14.0+cpu）
- 下载并编码两套语料：TinyShakespeare（100 万 token / 词表 65）、中文诗词（140 万 token / 词表 8300）
- 修掉一个真 bug：`GPT.generate` 的 KV Cache 路径从没用完整 prompt 预热，导致模型只看到 1 个 token 就续写

**Agent 侧的验证结论（学员尚未参与，故不标为掌握）**
- 全仓编译 0 失败
- `tests/test_common.py` 61/61 通过
- `tests/test_lessons.py` 9/9 通过（9 讲 `--quick` 全跑通，约 2.3 分钟）
- 09 讲实测：KV Cache 与全量前向逐 token 完全相同（不同位置数 = 0），加速最高 21 倍

**还没想通的**
- `out/` 里只有 40 步的测试模型，生成是乱码 —— 还没跑过完整训练
- 两个消融实验在 quick 模式下结论会被随机性淹没，需要在足够步数下重跑

**下一步**
- 跑一次完整训练，走完第一个「预测 → 对账」循环
