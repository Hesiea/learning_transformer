# 学习进度表

> 这份文件是 Agent 的「读心术」：每次开场它先读这里，就知道你在哪、该往哪推。
> **规则：升档必须有证据，证据写进 `log.md` 或 `experiments/`。**
> 没有证据的「我懂了」不算数——那是本次学习最需要警惕的错觉。

## 状态标记

| 标记 | 含义 | 升档需要什么 |
|---|---|---|
| ⬜ | 未开始 | — |
| 🟡 | 读过 / 看过代码 | 无 |
| 🟠 | 能复述（合上代码讲清原理） | `log.md` 里用自己的话写一段 |
| 🟢 | 能预测（改一个变量，先预判再实测） | 一条 `experiments/` 记录 |
| 🔵 | 能迁移（新场景用出来） | 一个自建小项目 |

---

## A. 核心概念（01~09 讲）

| # | 知识点 | 状态 | 证据 |
|---|---|---|---|
| A1 | 语言模型任务 = 预测下一个 token，标签是文本自身右移一位 | ⬜ | |
| A2 | 手写 softmax 与交叉熵，能解释为何初始 loss ≈ ln(V) | ⬜ | |
| A3 | 了解 bigram：上下文窗口为 1 的查表模型，是「地板成绩」 | ⬜ | |
| A4 | 注意力的置换等变性 → 为什么必须注入位置信息 | ⬜ | |
| A5 | 能解释可学习位置嵌入与正弦编码的取舍 | ⬜ | |
| A6 | 能推导 Attention(Q,K,V) = softmax(QKᵀ/√d_k + mask)V | ⬜ | |
| A7 | 能解释 1/√d_k 若不除会发生什么（并能设计实验证伪） | ⬜ | |
| A8 | 因果掩码：为什么 GPT 必须屏蔽未来，BERT 为什么不用 | ⬜ | |
| A9 | 多头：切分不增加 q/k/v 参数量，输出投影为何必要 | ⬜ | |
| A10 | 残差：d(x+f(x))/dx = 1 + f'(x)，梯度高速公路 | ⬜ | |
| A11 | LayerNorm 在 C 维归一化 → 不依赖 batch，适合自回归 | ⬜ | |
| A12 | 参数量分布：MLP 约占 2/3，注意力约 1/3 | ⬜ | |
| A13 | Pre-LN vs Post-LN 的排列差异与训练稳定性 | ⬜ | |
| A14 | 权重共享（wte ↔ lm_head）省多少、何时有效 | ⬜ | |
| A15 | 初始 loss ≈ ln(vocab_size) 作为自检指标 | ⬜ | |
| A16 | warmup + 余弦退火各自解决什么问题 | ⬜ | |
| A17 | 分组 weight decay：bias 与 LayerNorm 参数为何不衰减 | ⬜ | |
| A18 | 验证集 loss 与过拟合判据 | ⬜ | |
| A19 | 采样策略：temperature / top-k / top-p 的作用边界 | ⬜ | |
| A20 | KV Cache 原理与逐 token 等价性（本工程已实测一致） | ⬜ | |
| A21 | KV Cache 的复杂度收益与显存代价 | ⬜ | |

## B. 实操能力

| # | 能力 | 状态 | 证据 |
|---|---|---|---|
| B1 | 能用 `common/gpt.py` 的组件搭一个自己的模型变体 | ⬜ | |
| B2 | 能写一个受控的消融实验（单变量、多种子、固定种子） | ⬜ | |
| B3 | 能用 `model(x, return_weights=True)` 观察注意力并画热力图 | ⬜ | |
| B4 | 能读懂 checkpoint 结构并加载模型做推理 | ⬜ | |
| B5 | 能诊断训练异常（loss 不降 / 发散 / 过拟合） | ⬜ | |
| B6 | 能自己加一个结构改动并评估收益 | ⬜ | |
| B7 | 能用 git 记录实验（提交信息写清假设与结论） | ⬜ | |

## C. 元技能（做研究的底层能力）

| # | 能力 | 状态 | 证据 |
|---|---|---|---|
| C1 | 实验前先写预测，事后做对账 | ⬜ | |
| C2 | 能识别混淆变量（例如改 n_head 时 head_size 同步变了） | ⬜ | |
| C3 | 知道小步数结论不可信，会做多种子重复 | ⬜ | |
| C4 | 能区分「训练不足」与「过拟合」 | ⬜ | |
| C5 | 能提出 Agent 没提示过的问题 | ⬜ | |

## D. 未做的工程事项（做完才谈得上完整）

| # | 事项 | 说明 |
|---|---|---|
| D1 | 跑一次完整训练 | 目前 `out/` 只有快速测试模型（40 步），生成的是乱码 |
| D2 | 消融实验在足够步数下重跑 | `--steps 800` 以上、`--seeds 3`；quick 模式结论会被随机性淹没 |
| D3 | 中文语料训练 | 语料已知简繁混用，可做统一前后对比 |
| D4 | 扩展结构 | RoPE / RMSNorm / SwiGLU / BPE（见 README 第八节） |
| D5 | 推理服务化 | 把 09 讲的接口做成带吞吐测试的小服务 |

---

## 当前建议的下一步

**下一次会话（30~60 分钟）做这两件事**：

1. 跑一次完整训练，拿到可用权重（`src\07_training.py --preset tiny --steps 1200`，约 3~5 分钟）
2. 走完第一个「预测 → 对账」循环：**先写下你预期的最佳 val loss 是多少**，跑完再对账

做完后回填 A15/A18 的状态与证据，并在 `experiments/0001` 留下记录。

> 注意：跑训练需要 shell 权限。当前沙箱是 `workspace-write`，会拒绝访问本目录，
> 所以会话开头请直接说明「本轮要跑实验」。
