# 待解决问题清单

> 用途：学习过程中冒出来的「先记下来、别当场岔开」的问题。
> 每次会话结束时 Agent 会往里加 1~3 条；每次开场可以挑一条来查。
>
> 状态：🔍 待查 ｜ 🔬 正在做实验 ｜ ✅ 已解决（写明结论与证据） ｜ 🅿️ 暂缓

## 模板

```
### Q{N} ｜ 状态标记 ｜ 提出的日期
**问题**：
**为什么会想到这个**：
**打算怎么查**：（读代码 / 设计实验 / 查论文）
**结论**：（解决后回填，注明证据出处）
```

---

## 清单

### Q1 ｜ 🔍 待查 ｜ 2026-09-13

**问题**：如果去掉 `1/√d_k` 缩放，训练早期到底会发生什么？是 loss 直接爆炸，
还是只是收敛变慢？在**这个小模型**上（n_layer=4, n_embd=128）差异有多大？

**为什么会想到这个**：03 讲用数值说明了「softmax 会饱和成 one-hot」，
但那是**随机初始化的单个 batch** 上的现象，不等于训练动态的结论。

**打算怎么查**：把 `common/attention.py` 里的缩放系数改成可配置，
同种子跑对照实验（需要足够步数，quick 模式的差异会被随机性淹没）。

**结论**：

---

### Q2 ｜ 🔍 待查 ｜ 2026-09-13

**问题**：中文语料的词表是 8300，英文是 65。`ln(V)` 基准差了 2.2 左右，
所以**中英文的 loss 数值不可直接比较**。那要比较「模型在两种语言上学得好不好」，
应该用什么指标？

**为什么会想到这个**：README 里提了一句「别直接横向比 loss」，但没说该用什么。

**打算怎么查**：考虑「每字符比特数」（bits per character）之类的归一化指标，
推导一下交叉熵与它的换算关系，再用两套语料实测。

**结论**：
